var dez = 10;
var vinte = 20;



if (i) {
    let cinquenta;
    if (i) {
        let trinta = 30;
        cinquenta = 50;
    }
}





console.log(dez);
//document.body.innerHTML = document.body.innerHTML + dez;
let meuValor = 3;

if (condition) {
    
} else if (condition) {
    
} else if (condition) {
    
} else if (condition) {
    
} 



switch (meuValor) {
    case 3: {
        s += "caso3"
        break;
    }
    case 2: {
        s += "caso 12"
        break;
    }
    case 4: {
        s += "caso 4"
        break;
    }
    default:
        s += "5"
        break;
}



function soma (param1, param2) {

    return param1 + param2;
}

let r = soma(meuValor, 3);


let s = "";
let n = 1;

while(n < 10){
    n++;
    if (n === 5) {
        continue;
    }
    s += String(n)
}

let lista = [1, 2, 3, 4, 5]

let list = ["meu", "gato", "é", "azul"]
let lis2 = ""

for (let i = 0; i < list.length; i++) {
    let letra = list[1]
    if (list === "c") {
        lis2 += list.toUpperCase()
    } else {
        lis2 += list
    }
}


for(let i = 0, j = 1; i < 10; i++) { // podemos manipular +1 var de 1x usando a , 
    if(i === 5) {
        console.log("chegamos ao 5")
    }
}

// break funciona no swift e nas repetições (while, for)
document.body.innerText = s;